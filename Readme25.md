Adding function rewrite the Gross pay project (project3) program to give the employee 1.5 times the hourly rate for hours worked above 40 hours 

Here again the program prompts the user for hours and rate per hour to compute gross pay, you need to take into account that the result has exactly two digits after the decimal place.

Hint : overtime = hour-40

Additionally, we need to create a function which checks if the type of input is a float or not. If the values for hour or rate is not float we will return error